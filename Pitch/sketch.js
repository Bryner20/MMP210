var currentSlide = 1;
var totalSlide = 5;

function setup() {
    createCanvas('Navy');
}

function draw() {
    background(51);

    // slides
    if (currentSlide = 1) {
        //title
        textSize(100);
        text('A landscape planet', width/2, height/2);
    }
}